# Commands

- `python3`
- Virtual env available at `venv/bin/activate`

# Documentation

Each time you implement a new feature, write a markdown document in `docs/` summarizing the improvement.
The name of the file should be `001-my-new-feature.md` and should help to know what are the changes.

